import React from 'react';
import { Text, StyleSheet, View } from 'react-native';

const Cita = ({cita}) => {
    return (
        <View>
            <Text>{cita}</Text>
        </View>
    );
}
const styles = StyleSheet.create({
    
})  
export default Cita;
